import * as colors from './colors';

export const LIGHT_ON_DARK = {
  color:           colors.MONOCHROME.LIGHT,
  backgroundColor: colors.MONOCHROME.DARK,
};

export const DARK_ON_LIGHT = {
  color:           colors.MONOCHROME.DARK,
  backgroundColor: colors.MONOCHROME.LIGHT,
};
